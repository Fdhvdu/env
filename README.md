# .gitalias
add
```
[include]
	path = ~/env/.gitalias
```
in `~/.gitconfig`

# .tmux.conf
add `source-file ~/env/.tmux.conf` in `~/.tmux.conf`

# shell
execute `git submodule update --init --depth 1` first

add
```
. /path/to/git/contrib/completion/shell
```
in shell
